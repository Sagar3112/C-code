// Assignment operators

#include<stdio.h>

int main(int argc, char const *argv[])
{
    int a=5, b=3;
    a += b; // it's a = a+b;
   // a -= b; // it's a = a-b;
   // a *= b; // it's a = a*b;
   // a /= b; // it's a = a/b;     
    printf("%d \n", a);

    return 0;

    
}
